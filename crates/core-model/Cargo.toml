[package]
name = "bestterm-core-model"
description = "BestTerm's session tree: folders, sessions, and folder-to-session setting inheritance"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true
uuid.workspace = true

[dev-dependencies]
# The tree is persisted as TOML, so round-trip tests use the real format rather
# than a convenient stand-in.
toml.workspace = true
