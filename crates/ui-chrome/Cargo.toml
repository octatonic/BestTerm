[package]
name = "bestterm-ui-chrome"
description = "BestTerm application chrome: theme, ribbon, sidebar, tab bar, status bar"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
# The dialog produces a real `ProtocolConfig` rather than a parallel type of its own, so the
# session model has exactly one definition. See src/session_dialog.rs.
bestterm-core-model.workspace = true
egui.workspace = true
